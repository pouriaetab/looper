# LOOPER Python backend dependencies (the React frontend lives in ./frontend)
requests>=2.31      # HTTP calls to the Massive API
fastapi>=0.110      # JSON API consumed by the React frontend
uvicorn>=0.29       # ASGI server to run the FastAPI app
